FROM python:3.12-slim

RUN pip install --no-cache-dir \
    radon \
    pylint \
    pytest \
    scipy \
    pandas

WORKDIR /workspace

# No CMD — container stays alive via "sleep infinity" from DockerSandbox
